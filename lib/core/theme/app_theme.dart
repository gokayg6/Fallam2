import 'package:flutter/material.dart';

class AppTheme {
  AppTheme._();
  // Animation constants
  static const Duration durationFast = Duration(milliseconds: 50);
  static const Duration durationNormal = Duration(milliseconds: 100);
  static const Curve curveSpring = Curves.easeOutBack;
}
